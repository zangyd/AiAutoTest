# 前端服务配置
TEST_FRONTEND_HOST=localhost
TEST_FRONTEND_PORT=5173
TEST_FRONTEND_PROTOCOL=http
TEST_START_COMMAND=npm run dev

# 测试配置
TEST_WAIT_TIMEOUT=30
TEST_SKIP_SERVER_START=false

# 说明：
# TEST_FRONTEND_HOST: 前端服务主机地址
# TEST_FRONTEND_PORT: 前端服务端口
# TEST_FRONTEND_PROTOCOL: 前端服务协议(http/https)
# TEST_START_COMMAND: 启动前端服务的命令
# TEST_WAIT_TIMEOUT: 等待服务就绪的超时时间（秒）
# TEST_SKIP_SERVER_START: 是否跳过启动前端服务（当前端服务已在其他地方启动时使用） 